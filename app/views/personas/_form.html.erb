<div class="container">
  <%= form_for(@persona) do |f| %>
    <% if @persona.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@persona.errors.count, "error") %> prohibited this persona from being saved:</h2>

        <ul>
        <% @persona.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group">
      <%= f.label :rut %>
      <%= f.text_field :rut, class: 'form-control' %>
    </div>
    <div class="field">
      <%= f.label :rut %><br>
      <%= f.text_field :rut, class: 'form-control' %>
    </div>
    <div class="field">
      <%= f.label :nombre %><br>
      <%= f.text_field :nombre, class: 'form-control' %>
    </div>
    <div class="field">
      <%= f.label :cantidad %><br>
      <%= f.text_field :cantidad, class: 'form-control' %>
    </div>
    <div class="actions">
      <%= f.submit "Editar Alumno", class: 'btn btn-success' %>
    </div>
  <% end %>
</div>
